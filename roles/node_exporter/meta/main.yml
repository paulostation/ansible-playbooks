---
galaxy_info:
  author: paulao
  description: |
    Idempotently install prometheus/node-exporter from upstream tarball
    and configure systemd + ufw rule for tailnet-only access.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm, trixie]
    - name: Archlinux
      versions: [any]
dependencies: []
