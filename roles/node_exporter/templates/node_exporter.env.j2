# /etc/default/node_exporter — managed by ansible
# Extra command-line flags appended to the systemd ExecStart.
{% if node_exporter_extra_args | length > 0 %}
EXTRA_ARGS="{{ node_exporter_extra_args | join(' ') }}"
{% else %}
EXTRA_ARGS=""
{% endif %}
