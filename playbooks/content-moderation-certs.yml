---
- name: Deploy Content Moderation Pipeline certificates
  hosts: omarchy-mouse
  become: true
  roles:
    - role: content_moderation_certs
      vars:
        cm_step_ca_provisioner_password: "{{ cm_step_ca_provisioner_password }}"
