---
- name: Deploy Content Moderation Pipeline certificates
  hosts: omarchy-mouse
  become: true
  roles:
    - role: content_moderation_certs
      vars:
        cm_step_ca_provisioner_password: "{{ vector_step_ca_provisioner_password }}"
