---
- name: Setup QEMU and Libvirtd
  hosts: all
  gather_facts: false

  pre_tasks:
    - include_role:
        name: config_loader

  roles:
    - role: qemu-kvm
      tags:
        - qemu-kvm

    #- role: ansible-pulseaudio
    #  become: true
    #  tags:
    #    - pulseaudio
