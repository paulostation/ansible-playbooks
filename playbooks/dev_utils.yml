---
- name: playbook de teste
  hosts: all

  roles:
    - role: dev.utils
      become: true
      tags:
        - dev

    # - role: syncthing
    #   become: true
    #   tags:
    #     - syncthing

    # - role: vscode
    #   become: true
    #   tags:
    #     - dev
    #     - vscode

    # - role: geerlingguy.docker
    #   become: true
    #   tags:
    #     - docker

    # - role: slack
    #   become: true
    #   tags:
    #     - dev

    # - role: indicator-sysmonitor
    #   become: true
    #   tags:
    #     - utils
